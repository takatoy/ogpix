export interface CustomStyle {
  bg?: string;
  color?: string;
  accent?: string;
  fontSize?: string;
  logo?: string;
  tag?: string;
  pattern?: "dots" | "grid" | "diagonal" | "none";
  borderRadius?: string;
}
